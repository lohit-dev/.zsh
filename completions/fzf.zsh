    # Ctrl-R fzf completion
    # Commented out because fzf is not installed
    # To enable, install fzf: brew install fzf
    # if command -v fzf &>/dev/null; then
    #     eval "$(fzf --zsh)"
    # fi
